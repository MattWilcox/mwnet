# Keep node modules out of GitHub and have them be fetched at build time
node_modules

# Don't store the generated output in GIT
_publish

### Allow a place to stash related files locally that don't end up in GIT, e.g. source fonts, visuals, etc ###
/_gitignore/*
!/_gitignore/.gitkeep

### OSX stuff no one wants ###
.DS_Store
.AppleDouble
.LSOverride
._*

### Vim stuff no one wants ###
[._]*.s[a-w][a-z]
[._]s[a-w][a-z]
*.un~
Session.vim
.netrwhist
*~
